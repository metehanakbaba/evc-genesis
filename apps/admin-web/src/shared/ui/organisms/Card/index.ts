/**
 * Card Organism Component
 *
 * Unified card component that replaces duplicated card implementations.
 * Composed from atomic components while maintaining all existing variants.
 * Part of the atomic design system.
 */

export type { CardProps } from './Card';
export { Card } from './Card';
